[outputs]
# Homepage reaches Glances by its internal Compose service name.
webui_allowed_hosts=glances,glances:61208,localhost,127.0.0.1
# Glances is not published on the host; these are the only expected UI origins.
cors_origins=http://homepage:3000,http://192.168.1.23:3000
